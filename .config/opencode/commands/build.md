---
description: Build/fix a Linear issue end-to-end (read → spec if thin → implement → PR)
---

Invoke the `implementing-linear-issue` skill to deliver a Linear issue for the
current repo: read it, spec it first if it's thin, then implement and open a PR.

**Issue**: $ARGUMENTS

If no issue is given, pick the top `Todo` issue in this repo's Linear project
(mapping table in the global `AGENTS.md`).
